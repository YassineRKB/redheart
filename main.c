#include "libs.h"
/**
 * main - Entry point for Red
*/
int main()
{
	size_t hashlen = MD5_DIG_LEN;
	const char *TargetFile, *KnownHash, md5Hash[(hashlen * 2 + 1)];

	// check args len
	// init variables with data
	// compare hash
	return (EXIT_SUCCESS);
}